# AWS Configuration
# [required] AWS Region where your Cognito User Pool is located
NEXT_PUBLIC_AWS_REGION=us-east-1

# Amazon Cognito Configuration
# [required] Cognito User Pool ID - find this in AWS Console > Cognito > User Pools
# Format: <region>_<alphanumeric> (e.g., us-east-1_abc123XYZ)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# [required] Cognito App Client ID - find this in your User Pool > App Integration > App clients
# Format: 26-character alphanumeric string
NEXT_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX

# [required] Cognito Domain for Hosted UI - find this in User Pool > App Integration > Domain
# Format: <domain-prefix>.auth.<region>.amazoncognito.com
# Example: collectiq-dev.auth.us-east-1.amazoncognito.com
NEXT_PUBLIC_COGNITO_DOMAIN=collectiq.auth.us-east-1.amazoncognito.com

# OAuth Configuration
# [required] OAuth Redirect URI - where Cognito redirects after successful authentication
# IMPORTANT: Must point to root (/) for AWS Amplify to handle callback automatically
# This URL must be added to "Allowed callback URLs" in Cognito App Client settings
# Development: http://localhost:3000/
# Production: https://app.collectiq.com/
NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://localhost:3000/

# [required] OAuth Logout URI - where Cognito redirects after sign out
# This URL must be added to "Allowed sign-out URLs" in Cognito App Client settings
# Development: http://localhost:3000
# Production: https://app.collectiq.com
NEXT_PUBLIC_OAUTH_LOGOUT_URI=http://localhost:3000

# API Configuration
# [required] Backend API base URL (API Gateway endpoint)
# Development: Your API Gateway URL (e.g., https://abc123.execute-api.us-east-1.amazonaws.com)
# Production: https://api.collectiq.com
NEXT_PUBLIC_API_BASE=https://api.collectiq.com

# Optional: Feature Flags
FEATURE_FLAGS=

# Optional: Analytics
NEXT_PUBLIC_ANALYTICS_ID=

# Optional: Error Tracking
SENTRY_DSN=
